<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/worknutri_icon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/worknutri_icon_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NutriCoop"
        tools:targetApi="31">
        <activity
            android:name=".ui.agendasFragment.ScheduleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.formularios.formularioPaciente.PatientFormActivity"/>
        <activity android:name=".ui.formularios.clinicForm.ClinicFormActivity"/>
        <activity android:name=".ui.detail.detailPaciente.PacienteDescriptionActivity"/>
        <activity android:name=".ui.detail.detailClinica.ClinicaDescriptionActivity"/>
        <activity android:name=".ui.ActivityToTest" />

    </application>

</manifest>